╔══════════════════════════════════════════════════════════════════╗
║  ⚠️  AÇÃO NECESSÁRIA - EXECUTAR SQL NO SUPABASE                  ║
║  Erro: coluna 'tipo_documento' não encontrada                    ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ 🔴 PROBLEMA DETECTADO                                            │
└──────────────────────────────────────────────────────────────────┘

Erro: "Could not find the 'tipo_documento' column"

Causa: A coluna tipo_documento ainda não existe no banco de dados.
       Você tentou enviar um Informe de IR, mas o sistema ainda não
       está preparado no banco.

┌──────────────────────────────────────────────────────────────────┐
│ ✅ SOLUÇÃO - SIGA ESTES PASSOS                                   │
└──────────────────────────────────────────────────────────────────┘

PASSO 1: Abrir Supabase
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Abra o navegador
2. Vá para: https://supabase.com/dashboard
3. Faça login
4. Selecione seu projeto (ISIBA)

PASSO 2: Abrir SQL Editor
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. No menu lateral esquerdo, clique em: 🗂️ "SQL Editor"
2. Clique em "+ New query" (botão verde no topo)

PASSO 3: Copiar o SQL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Copie APENAS o código SQL abaixo (sem os ═══):

═══════════════════════════════════════════════════════════════════
-- Adicionar coluna tipo_documento
ALTER TABLE contracheques 
ADD COLUMN IF NOT EXISTS tipo_documento VARCHAR(50) DEFAULT 'contracheque';

-- Atualizar registros existentes
UPDATE contracheques 
SET tipo_documento = 'contracheque' 
WHERE tipo_documento IS NULL;

-- Adicionar constraint
DO $$ 
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_constraint 
        WHERE conname = 'contracheques_tipo_documento_check'
    ) THEN
        ALTER TABLE contracheques 
        ADD CONSTRAINT contracheques_tipo_documento_check 
        CHECK (tipo_documento IN ('contracheque', 'informe_ir'));
    END IF;
END $$;

-- Criar índice
CREATE INDEX IF NOT EXISTS idx_contracheques_tipo_documento 
ON contracheques(tipo_documento);

-- Verificar se funcionou
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'contracheques' 
  AND column_name = 'tipo_documento';
═══════════════════════════════════════════════════════════════════

PASSO 4: Colar e Executar
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Cole o código SQL no editor
2. Clique em "RUN" (botão verde no canto inferior direito)
3. Aguarde a execução (deve levar 1-2 segundos)

PASSO 5: Verificar Resultado
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Você deve ver este resultado na última query:

┌─────────────────┬───────────┬────────────────────┐
│ column_name     │ data_type │ column_default     │
├─────────────────┼───────────┼────────────────────┤
│ tipo_documento  │ varchar   │ 'contracheque'     │
└─────────────────┴───────────┴────────────────────┘

Se aparecer esta linha → ✅ SUCESSO! Pode continuar

Se der erro → ❌ Copie o erro e me envie

┌──────────────────────────────────────────────────────────────────┐
│ 🔄 DEPOIS DE EXECUTAR O SQL                                      │
└──────────────────────────────────────────────────────────────────┘

1. Volte para o Painel RH
2. Pressione Ctrl + Shift + R (recarregar sem cache)
3. Tente enviar o Informe de IR novamente
4. Deve funcionar! ✅

┌──────────────────────────────────────────────────────────────────┐
│ 📸 CAPTURAS DE TELA (REFERÊNCIA)                                 │
└──────────────────────────────────────────────────────────────────┘

Supabase Dashboard:
┌────────────────────────────────────────────────────────┐
│  ISIBA Project                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📊 Home                                         │   │
│  │ 🗂️  SQL Editor  ← CLIQUE AQUI                  │   │
│  │ 📋 Table Editor                                 │   │
│  │ 🔐 Authentication                               │   │
│  │ 📦 Storage                                      │   │
│  └─────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────┘

SQL Editor:
┌────────────────────────────────────────────────────────┐
│  + New query  [Saved queries ▼]                       │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ALTER TABLE contracheques                        │ │
│  │ ADD COLUMN IF NOT EXISTS tipo_documento...       │ │
│  │                                                  │ │
│  │ [Cole o SQL aqui]                                │ │
│  │                                                  │ │
│  └──────────────────────────────────────────────────┘ │
│                                        [▶ RUN] ← CLIQUE│
└────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ ❓ DÚVIDAS COMUNS                                                │
└──────────────────────────────────────────────────────────────────┘

Q: Posso executar tudo de uma vez?
A: SIM! Copie todo o bloco SQL e execute. O script tem proteções
   (IF NOT EXISTS) para evitar erros.

Q: Vai afetar os contracheques já enviados?
A: NÃO! O script marca automaticamente todos os existentes como
   'contracheque'. Nada será perdido.

Q: Preciso fazer backup antes?
A: Recomendado, mas o script é seguro. Ele apenas ADICIONA uma 
   coluna, não remove nem modifica dados existentes.

Q: E se der erro de permissão?
A: Você precisa ser o owner do projeto no Supabase. Se não for,
   peça para o administrador do Supabase executar.

Q: Quanto tempo leva?
A: 1-3 segundos. É muito rápido!

┌──────────────────────────────────────────────────────────────────┐
│ 🎯 CHECKLIST                                                     │
└──────────────────────────────────────────────────────────────────┘

[ ] 1. Abri o Supabase Dashboard
[ ] 2. Cliquei em "SQL Editor"
[ ] 3. Cliquei em "+ New query"
[ ] 4. Copiei o SQL do bloco acima
[ ] 5. Colei no editor
[ ] 6. Cliquei em "RUN"
[ ] 7. Vi a confirmação de sucesso
[ ] 8. Voltei ao Painel RH
[ ] 9. Recarreguei a página (Ctrl+Shift+R)
[ ] 10. Testei enviar Informe de IR → FUNCIONOU! ✅

┌──────────────────────────────────────────────────────────────────┐
│ 🆘 SE PRECISAR DE AJUDA                                          │
└──────────────────────────────────────────────────────────────────┘

1. Tire um print do erro que aparece no Supabase
2. Copie a mensagem de erro completa
3. Me envie para eu ajudar

Ou simplesmente me diga: "Deu erro ao executar o SQL"

┌──────────────────────────────────────────────────────────────────┐
│ ⚡ QUICK START (SE TIVER PRESSA)                                 │
└──────────────────────────────────────────────────────────────────┘

1. Supabase → SQL Editor → New query
2. Cole este SQL e execute:

ALTER TABLE contracheques 
ADD COLUMN IF NOT EXISTS tipo_documento VARCHAR(50) DEFAULT 'contracheque';

UPDATE contracheques SET tipo_documento = 'contracheque' 
WHERE tipo_documento IS NULL;

3. Pronto! Pode usar o sistema.

═══════════════════════════════════════════════════════════════════
IMPORTANTE: Sem executar este SQL, o sistema NÃO vai funcionar
            para enviar Informes de IR!
═══════════════════════════════════════════════════════════════════
